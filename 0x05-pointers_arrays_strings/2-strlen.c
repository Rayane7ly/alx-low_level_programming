#include <stdio.h>
#include <string.h>
#include "main.h"

/**
 * _strlen - return lenght of a string
 *
 * @s: variable lenght to be calculated
 *
 * Return: return "a" the lenght of s
*/
int _strlen(char *s)
{
	int a;

	a = strlen(s);
	return (a);
}
